[project]
name = "scanner-shared"
version = "0.1.0"
description = "Enums, naming conventions et contrats partagés"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.7",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["scanner_shared"]
